{
  "name": "task-manager",
  "private": true,
  "scripts": {
    "dev": "concurrently -n SRV,CLI \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "sonner": "^2.0.7"
  }
}
